using System;
using System.Linq;

namespace piSensorNet.Web.Custom
{
    public static class Constants
    {
        public const string DateTimePattern = "dd/MM/yyyy, HH:mm:ss.fff";
        public const string DateTimeFormat = "{0:" + DateTimePattern + "}";
    }
}
